{
  "name": "aios-3d-brain",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "start": "node serve.mjs",
    "build:graph": "node build.mjs",
    "build:js": "esbuild src/app.js --bundle --format=esm --minify --outfile=dist/app.js --target=es2022",
    "build": "npm run build:js && npm run build:graph"
  },
  "dependencies": {
    "3d-force-graph": "^1.80.0",
    "dompurify": "^3.4.15",
    "marked": "^18.0.11",
    "three": "0.185.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
