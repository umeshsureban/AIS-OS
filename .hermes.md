# Brain Vault — Hermes Agent Configuration

> This file is auto-loaded by Hermes Agent when working in this directory or any subdirectory.
> It configures the brain vault as the centralized memory system for AITOmate Systems.

## Brain Vault Identity

This is the **centralized memory vault** for AITOmate Systems. All AI tools (Hermes Agent, Claude Code, Codex) read and write this vault. The vault is backed by GitHub at `umeshsureban/AIS-OS`.

## Vault Structure

```
brain-vault/
├── CLAUDE.md, AGENTS.md    ← Tool routing files
├── MEMORY.md               ← Shared memory (all tools)
├── brain.yaml              ← Authority boundaries
├── context/                ← About Umesh ji, business, clients
├── references/             ← 3Ms framework, 4Cs framework
├── decisions/              ← Append-only decision log
├── projects/               ← Client project files
├── raw/                    ← Drop source material
├── wiki/                   ← Auto-generated pages
└── .claude/skills/         ← /onboard, /audit, /level-up
```

## Memory Rules

1. **MEMORY.md is the shared memory.** Always check it first for:
   - Active deals and prospects
   - Client statuses and contact info
   - Lessons learned
   - Communication preferences

2. **Projects folder has client details.** Check `projects/` for:
   - Full client information
   - Deal stages and probabilities
   - Action items and next steps

3. **Context folder has business info.** Check `context/` for:
   - About Umesh ji
   - AITOmate Systems overview
   - All clients and prospects summary

4. **Decisions are append-only.** Never edit `decisions/log.md` — only append.

5. **Authority boundaries are in brain.yaml.** Respect write prefixes.

## Sync Rules

- **Nightly sync at 18:30 UTC** → git pull → git add → git commit → git push
- **After every meaningful write** → update MEMORY.md if deals/clients change
- **End-of-session write-back** → append learnings to decisions/log.md

## Communication Preferences

- Address as **"Umesh ji"** — never "Sir Ji" or "Surji"
- Respectful Hindi: "आप" + "Umesh ji"
- Respectful Kannada: "ನೀವು" + "Umesh ji"
- Never informal ("तुम"/"ನಿನ್ನு")
- Direct action over excessive confirmation
- Verify data myself rather than asking

## Active Deals (as of Sep 4, 2026)

1. **Anand Hiremath** (AU, Education SaaS) — 80%, demo sent, awaiting reply
2. **Kumaraswamy** (IN, Spiritual Guru) — 20%, scheduling meeting

## Lessons Learned

- YouTube uploads via Composio MCP require H.264 Baseline Profile 3.0
- Instagram posting via Composio MCP fails — S3 staging URLs not accessible
- Host VPS crontab was empty — YouTube sync broken
